# Convex Deployment
CONVEX_DEPLOYMENT=dev:your-deployment-name
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Convex Site URL (same as VITE_CONVEX_URL but ends in .site)
VITE_CONVEX_SITE_URL=https://your-deployment-name.convex.site

# Your local site URL
VITE_SITE_URL=http://localhost:3000

# OpenAI API Key (for AI features)
OPENAI_API_KEY=sk-your-key-here

# =====================
# Convex Environment Variables
# Set these on your Convex deployment using:
#   npx convex env set VARIABLE_NAME value
# =====================

# Better Auth Secret (set on Convex deployment)
# Generate with: openssl rand -base64 32
# npx convex env set BETTER_AUTH_SECRET <your-secret>

# Site URL (set on Convex deployment)
# npx convex env set SITE_URL http://localhost:3000

# Google OAuth (optional - set on Convex deployment)
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# npx convex env set GOOGLE_CLIENT_ID <your-client-id>
# npx convex env set GOOGLE_CLIENT_SECRET <your-client-secret>
